Verify that Swagger specifications match their corresponding Pact interactions.

### How to

Add a `x-pact-interactions` key to a Swagger operation block.

Create a ScalaTest test that extends `SwagPactProviderSpec`.
